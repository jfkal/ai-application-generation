spring:
  application:
    name: ai-application-generation
server:
  port: 8123
  servlet:
    context-path: /api
# springdoc-openapi
springdoc:
  group-configs:
    - group: 'default'
      packages-to-scan: com.yuanfen.aiapplicationgeneration.controller
# knife
knife4j:
  enable: true
  setting:
    language: zh_cn
